{
  "private": true,
  "name": "design-system",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "pnpm --filter astra-components dev",
    "publish": "node scripts/publish.js",
    "release": "node scripts/release.js",
    "update:deps": "node scripts/update-deps.js"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
